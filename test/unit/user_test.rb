require 'test_helper'

class UserTest < ActiveSupport::TestCase
 # test "a user should enter a first name" do
  #user = User.new
 # assert !user.save
  #assert !user.errors[:first_name].empty?
  # test "the truth" do
  #   assert true
  #end
  # test "a user should enter a last name" do
  #user = User.new
  #assert !user.save
  #assert !user.errors[:first_name].empty?
  # test "the truth" do
  #end
  #test "a user should enter a profile name" do
  #user = User.new
  #assert !user.save
  #assert !user.errors[:first_name].empty?
  # test "the truth" do
  #
	#end
   #test "a user should hava a unique profile name" 
   	#user = User.new
   	#user.profile_name = 'ori'
   	#user.last_name = 'wininger'
   	#user.first_name = 'ori'
   	#user.email = 'example@example.com'
   	#user.password = 'hwghghghj'
   	#user.password_confirmation = 'hwghghghj'
   	#assert !user.save
   	#puts user.errors.inspect
   	#assert !user.errors[:profile_name].empty
	#end
	#test "a user should have a profile name without spaces"
	#user = User.new
	#user.profile_name = " horaj jarw"
	#assert !user.save
	#assert !user.errors[:profile_name].include?("must be formated corrrectly.")
end
